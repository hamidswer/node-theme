doctype html
html
    head
        title Main page #{title}
        link(rel='stylesheet' href='/css/style.css')
        link(rel='shortcut icon' type='image/svg+xml' href='/img/bookmark.svg')

    body
        h1 Hello world!!!! 
        p this is description
        h1=title 
        p=description.toUpperCase()
        - const x = 'hello'
        h2=x